Hashing :-
it means converting data into fixed index
key → hash function → index → store value
"a" → 97 →  index 2
"b" → 98 → index 3
time complexity is O(1)
it stores the data 
it access the  data quickly
avoid searching again and again
The conversion of data into the indexing is called hashing
Hash -Based Structures 
1.Hash Map
in this key → value
Example:- name to marks 
age - number
character-index
when we call roll number the students  stands like for every  value there is a key
for hash map
import java.util.HashMap;
Hashmap<Integer,string>map=new HashMap<>();
here integer is key
here string is value 
to insert the value we must use put()
map.put(1,"Car");
map.put(2,"Bike");
for retriving the data we must use get();
map.get(2);
here we get index of key 2 value is Bike
for checking if the value  is exists we must use contains.key();
map.contains.key(1);
for removing the data we must use map.remove();
for knowing the size of the map we must use map.size();
map.size()

EXAMPLE:-
To count the how many letters are there
import java.util.HashMap;

class Frequency {
    public static void main(String[] args) {

        String s = "aabbc";

        HashMap<Character, Integer> map = new HashMap<>();

        for (int i = 0; i < s.length(); i++) {

            char ch = s.charAt(i);

            if (map.containsKey(ch)) {
                map.put(ch, map.get(ch) + 1);
            } else {
                map.put(ch, 1);
            }
        }

        System.out.println(map);
    }
}
let us discuss about here HashMap 
we declare HasMap as HashMap<Integer,String>=new HasMap<>();
first we must import from java in the util hashmap import java.util.HashMap
then name the class  then satic method 
And then declare String
now we must use hahamap structureHashMAp<Character,Integer>=new HashMap<>();
here in mapping<>first comes key,then value its data type above we are using character by using character we are counting so it must be character then for counting we want integer
then for loop for(int i=0;i<s.length;i++){
for getting the char is used for indexing
char ch=s.charAt(i) this means declaring ch as charAt(i) means what the char at index i that is char  that too in string s
if(map.containsKey(ch)){ means we declared before ch now we are checking if character is present in the key means
if a already in map as a key  it says then map.put(ch,map.get(ch)+1); it keeps in the map buy put() ch is the key that charcter which is already therre that will come as map.get(ch) as a integer to that +1
if it is not there  then else mapp.put(ch,1) means we must put it in the ch,1 because if not there there is no need to the add the count 
for if we counted how many characters are there 
Dry Run
if a contains already in the map then put in the map as a,1+1 because map.get(ch) returns as count like a is already repeated so 1 
last print the map "," returns as = in HashMap
















